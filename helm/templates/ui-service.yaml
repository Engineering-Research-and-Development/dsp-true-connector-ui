{{- if .Values.ui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tc.fullname" . }}-ui
  namespace: {{ .Values.global.namespace }}
spec:
  selector: { app: {{ include "tc.fullname" . }}-ui }
  ports:
    - port: {{ .Values.ui.servicePort }}
      targetPort: 80
{{- end }}
